{
  "name": "roddy-acme-portal-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scenario example: a made-up product (Acme Portal) with its own login, embedding Roddy's web chat headlessly with visitor tokens",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "concurrently -n server,web -c blue,green \"node server/server.js\" \"vite\"",
    "dev": "npm start",
    "build": "vite build"
  },
  "dependencies": {
    "@ai-sdk/react": "^2.0.168",
    "ai": "^5.0.166",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.0",
    "vite": "^6.0.0"
  }
}
